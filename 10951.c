#include <stdio.h>

int main(void)
{
    int x, y;
    while (1)
    {
        scanf("%d %d", &x, &y);
        if (x == null || y == null)
            break;
        else
        {
            printf("%d\n", x + y);
        }
    }
    return 0;
}